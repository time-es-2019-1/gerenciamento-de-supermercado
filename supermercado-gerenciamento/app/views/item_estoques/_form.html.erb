<%= form_with(model: item_estoque, local: true) do |form| %>
  <% if item_estoque.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_estoque.errors.count, "error") %> prohibited this item_estoque from being saved:</h2>

      <ul>
      <% item_estoque.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :produto_id %>
    <%= form.collection_select(:produto_id , Produto.all,:id, :marca, prompt: true)%>

  </div>

  <div class="field">
    <%= form.label :quantidade %>
    <%= form.number_field :quantidade %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
